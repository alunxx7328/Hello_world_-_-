#!/bin/bash
echo "=== АНАЛИЗ ДИСКОВ ==="
df -h | awk 'NR>1 {
    print "Файловая система:", $1, "Заполнено:", $5
    gsub(/%/, "", $5)
    if ($5 > 90) print "⚠️ ПРЕДУПРЕЖДЕНИЕ: Диск", $1, "заполнен на", $5 "%!"
}'
